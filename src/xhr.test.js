var expect = require('chai').expect;
const { XHRRequest, XHRResponse } = require('./xhr.ts');

describe('XHR Implementation', () => {
    it('XHRRequest should exist', () => {
        expect(XHRRequest).to.be.defined
    })
    it('XHRResponse should exist', () => {
        expect(XHRRequest).to.be.defined
    })
})